# 진행 상황

> `orchestrator` 에이전트가 각 마일스톤 종료 시 이 파일을 업데이트합니다.

## 마일스톤 트래커

| ID | 이름 | 상태 | 담당 | 완료일 |
|---|---|---|---|---|
| M0 | 부트스트랩 | ✅ done | backend-api + frontend-ui | 2026-04-25 |
| M1 | 이미지 파이프라인 | ✅ done | image-pipeline | 2026-04-25 |
| M2 | 자동 편집 / 폴라로이드 | ✅ done | layout-engine | 2026-04-25 |
| M3 | Fabric.js 에디터 | ✅ done | fabric-editor | 2026-04-25 |
| M4 | 표지 에디터 | ✅ done | fabric-editor | 2026-04-25 |
| M5 | PDF 생성 | ✅ done | pdf-generator | 2026-04-25 |
| M6 | 주문·결제 | ✅ done | backend-api | 2026-04-25 |
| M7 | 관리자 콘솔 | ✅ done | admin-panel | 2026-04-25 |
| M8 | QA & 폴리싱 | ✅ done | qa-reviewer | 2026-04-25 |
| M9 | UX 보강 (toast/postcode/dark/usage-guard) | ✅ done | frontend-ui + admin-panel | 2026-05-05 |
| M10 | 법규 준수 + 배포 준비 | ✅ done | backend-api + frontend-ui | 2026-05-05 |
| M11 | 클립아트 영속화 + PDF 재시도 큐 + 감사 로그 | ✅ done | fabric-editor + backend-api + admin-panel | 2026-05-05 |
| M12 | 편집기 UX 보강 (reorder/추가삭제/복붙/단축키) | ✅ done | fabric-editor + frontend-ui | 2026-05-05 |
| M15 | 페이지 미리보기 + 이메일 인프라 (큐/템플릿/워커 stub) | ✅ done | pdf-generator + backend-api | 2026-05-05 |
| M16-7 | 카카오 OAuth 활성화 + 포인트 거래 내역(ledger) | ✅ done | backend-api + frontend-ui | 2026-05-09 |
| M16-종합 | 할인 코드 결제 적용 + 후기 작성 + 포인트 내역 페이지 마무리 | ✅ done | frontend-ui | 2026-05-13 |
| M8-보강 | PDF 런타임 검증 + Playwright E2E + Lighthouse 측정 | ✅ done | qa-reviewer | 2026-05-13 |
| M5-패치 | PDF borderRadius + shadow 동시 적용 시 그림자 클립 해소 (2-pass 분리) | ✅ done | pdf-generator | 2026-05-13 |
| M-홈리뉴얼 | 홈 §3 특징 / §4 사이즈 섹션을 사진 배경 카드 + fade-up 진입으로 리디자인 | ✅ done | frontend-ui | 2026-05-14 |
| M-내비최적화 | 페이지 이동 체감 속도 개선 (staleTimes / loading skeleton / RPC 단일화 / SW SWR / 정적 prerender) | ✅ done | frontend-ui + backend-api | 2026-05-14 |

상태 이모지: ⬜ pending · 🔄 in_progress · ✅ done · ⛔ blocked

## 최근 이벤트
- 2026-04-25: 프로젝트 계획 수립, 에이전트 9종 정의 완료.
- 2026-04-25: **M0 부트스트랩 완료**. 19개 파일 생성. QA 통과 (M 1건 즉시 수정, L 2건은 후속 마일스톤으로 이관).
- 2026-04-25: **M1 이미지 파이프라인 완료**. 16개 파일 생성 + package.json Edit. QA [H] 1건 즉시 수정 (complete 라우트에서 실제 blob 크기 재검증 추가 — 20MB 초과 시 객체 삭제+에러).
- 2026-04-25: **M2 자동 편집/폴라로이드 완료**. `lib/layout/**` (types/sort/polaroid/collage/templates/generate + 3종 테스트) + `/api/layout/generate` + `/api/pages` + `/editor/[projectId]` (page + EditorClient + TopBar + GenerateControls + PreviewGrid + PagePreview) 총 15개 파일 신설. 의존성 추가 없음.
- 2026-04-25: **M2 QA 후속 패치** — [M] `/api/pages` photos 조회에 `project_id` 스코프 추가 (IDOR 사전 차단). [M] 재생성 트랜잭션화: 마이그레이션 `0005_layout_rpc.sql` 추가하여 `regenerate_project_pages` RPC로 update+delete+insert를 단일 트랜잭션 처리 (`/api/layout/generate` 라우트도 RPC 호출로 교체, data-loss 방지). [L] TopBar 비활성 주문 버튼을 `<Button disabled>`로 교체. fabric-editor.md 직렬화 규약을 실제 PageDoc 스키마로 동기화 (M3 어댑터 위치 명시).
- 2026-04-25: **M3 Fabric.js 에디터 완료**. 의존성 `fabric@6.4.3`+`jsdom` 추가. `lib/fabric/{serialize,history,gestures,snap,fonts,url-refresher}.ts` + 2개 테스트. 컴포넌트 `FabricStage/Toolbar/SelectionPanel/ResourcePalette/CollageTemplateDialog`. API `/api/pages/[id]` (GET+PATCH, isPageDoc 검증 + bookSize/pageNo 일치) + `/api/resources`. 단일 페이지 편집 라우트 `/editor/[projectId]/pages/[pageId]`. 마이그레이션 `0006_pages_updated_at.sql`. 자동저장 5s + beforeunload guard.
- 2026-04-25: **M5 PDF 생성 완료**. 의존성 `@napi-rs/canvas`+`@pdf-lib/fontkit`+`pdf-lib` 추가, `next.config.ts` `serverExternalPackages` → `experimental.serverComponentsExternalPackages` 로 교체 (M0 이월 정리). `lib/pdf/{constants,photos,fonts,text-wrap,crop-mark,render-page,build,jobs}.ts` + `build.test.ts`. API `/api/pdf/build` (POST, target=interior|cover|all, 인라인 처리, signed URL 반환) + `/api/pdf/progress` (SSE, in-memory jobs 레지스트리). 마이그레이션 `0007_pdf_storage.sql` (private `pdfs` 버킷 + RLS — 사용자 자신의 `{userId}/...` 만 SELECT). 자체 PNG 렌더러 (Fabric 의존 없이 PageDoc → 300dpi PNG, mm/pt 좌표계, 한/영 혼합 word-wrap). pdf-lib 합성 + 4모서리 crop mark + 표지 책등 마크. 사진 LRU 캐시 (100MB). UI: `editor/[projectId]/PdfActions.tsx` (표지/내지/전체 다운로드 + 진행률 SSE).
- 2026-04-25: **M6 주문·결제 완료**. 의존성 `@tosspayments/payment-sdk@^1.9.1` 추가. 신규 — `lib/orders/{pricing,state}.ts` + 2개 테스트, `lib/payments/toss.ts` (confirm + fetch 헬퍼, TossError), `lib/pdf/build-job.ts` (M5 빌드 로직 추출 — `runProjectPdfBuild`). API — `/api/orders/create` (POST, Zod 검증 + 가격 재계산 + tossOrderId 발급), `/api/payments/confirm` (POST, 토스 검증 + amount 이중 검증 + status='paid' 전이 + 인라인 PDF 빌드 → orders.cover/interior_pdf_key), `/api/payments/webhook` (POST, optional X-Webhook-Secret + paymentKey 직접 조회로 amount 이중 검증). UI — `/order/[projectId]` (서버 prefetch + OrderForm 클라: 수량 stepper / 배송지 / 약관 / sticky CTA / 토스 SDK 동적 import), `/order/[projectId]/success` + SuccessClient (StrictMode dedupe + confirm 호출), `/order/[projectId]/fail`, `/mypage/orders`, `/mypage/orders/[orderId]` + OrderPdfButtons (signedUrl 발급), `/mypage` → orders 리다이렉트. M5의 `app/api/pdf/build/route.ts` 도 `runProjectPdfBuild` 호출로 리팩터(중복 제거). RLS 점검 — orders SELECT 사용자 본인, INSERT/UPDATE 는 service_role(`createAdminSupabase()`)로만. amount 위변조 방지 — 클라 amount 신뢰 X, 서버에서 재계산하고 토스 응답 totalAmount 와 비교. 멱등성 — confirm 재호출 시 동일 paymentKey + amount + status>=paid 면 success 반환. .env.example 갱신 (`TOSS_WEBHOOK_SECRET` 추가).
- 2026-04-25: **M7 관리자 콘솔 완료**. 의존성 `exceljs@^4.4.0` 추가. 마이그레이션 `0008_orders_tracking.sql` (tracking_no/tracking_carrier/shipped_at/delivered_at + idx_orders_status_paid_at) + `0009_resources_storage.sql` (`resources` 버킷 + RLS — service_role write/auth read). 신규 — `lib/admin/{auth,excel,resources}.ts` + `excel.test.ts`. 공통 — `components/admin/{StatCard,DataTable,StatusBadge,UploadDropzone}.tsx`. 페이지 — `/admin` (대시보드: 오늘 주문/결제/신규 7일/제작중 + 최근 5건), `/admin/book-sizes` (리스트/신규/[id] 편집 + 사용중 삭제 차단), `/admin/resources/[type]` (font/clipart/background 통합 — 업로드 드롭존 + active 토글 + meta 표시), `/admin/orders` (필터+페이지네이션 50건씩) + `/admin/orders/[id]` (상세 + 상태 전이 다이얼로그 + 송장 입력 + PDF 재생성), `/admin/orders/export` (Excel 미리보기+다운로드), `/admin/users` (역할 변경 — 자기강등 차단). API — `/api/admin/{book-sizes,resources,orders,users}/...` + `/api/admin/orders/[id]/transition` (assertTransition 사용) + `/api/admin/orders/[id]/rebuild-pdf` (runProjectPdfBuild 호출) + `/api/admin/orders/export` (exceljs 빌드 + Content-Disposition). `withAdmin` 래퍼로 모든 admin API 가 `requireAdmin` 통과(미들웨어 + 라우트 이중 보호). `app/admin/layout.tsx` 도 `requireAdmin` 호출. `/api/auth/sign-out` 추가 (사이드바 form action). Order 타입에 tracking 4컬럼 추가.
- 2026-04-25: **M4 표지 에디터 완료**. `lib/layout/{cover,cover-templates}.ts` (5종 템플릿) + 2개 테스트. 컴포넌트 `CoverSpineGuide/CoverTemplateDialog`. 표지 라우트 `/cover/[projectId]` + API `/api/cover` (GET/PATCH, isPageDoc + layoutMode=="cover" 검증). PageDoc에 `backgroundImage` 필드 확장 + serialize 양방향 + FabricStage `loadDoc` 통합. 책등 두께 자동 계산(pageCount × spine_formula).
- 2026-05-05: **M10 법규 준수 + 배포 준비 완료**. 의존성 추가 없음. 신규 — `app/(legal)/{layout,_components/LegalArticle,terms/page,privacy/page,refund/page}.tsx` (한국 표준 골격, prose 톤 + 인쇄 친화), `supabase/migrations/0010_account_deletion.sql` (profiles `deleted_at`/`deletion_reason`/`terms_agreed_at`/`privacy_agreed_at` + `anonymize_account(uuid,text)` / `record_agreements(uuid)` SECURITY DEFINER RPC, service_role 만 EXECUTE), `app/api/account/delete/route.ts` (이메일 재입력 검증 + pending/paid/in_production/shipped 차단 + RPC 익명화 + admin.deleteUser), `app/api/auth/agree/route.ts` (record_agreements RPC), `app/(user)/mypage/account/{page,DeleteAccountCard}.tsx` (프로필 카드 + 탈퇴 다이얼로그 — 사유 selectbox + 동의 체크), `app/(user)/mypage/page.tsx` 메뉴 그리드(주문/계정), `app/robots.ts` + `app/sitemap.ts` (admin/api/mypage/editor disallow, 정적 라우트만 색인), `vercel.json` (icn1 region + functions maxDuration=300/memory=1769 PDF·confirm·rebuild + Cache-Control no-store + 보안 헤더), `.env.production.example`. 수정 — `lib/auth/session.ts` `requireUser()` 가 profiles.deleted_at 검사 후 410 throw, `app/api/auth/callback/route.ts` 세션 교환 직후 record_agreements best-effort, `app/(auth)/login/LoginForm.tsx` 약관/개인정보 동의 체크박스(필수, 미체크 시 disabled), `app/api/health/route.ts` DB ping + env 체크 + 503 응답, `app/layout.tsx` metadataBase / openGraph / twitter / robots / keywords / formatDetection 보강, `components/layout/Footer.tsx` /refund 링크 + 강조, `lib/db/types.ts` Profile 4컬럼 + Functions 2 RPC 등록, `README.md` Vercel 배포 가이드 / 법적 페이지 / 오토파일럿 섹션 추가. 검증 — pnpm typecheck/lint/test(106 passed/1 skipped)/build 모두 통과. 후속 — 변호사 검토(약관/정책 텍스트), 카카오 OAuth provider 등록(LoginForm 주석 해제), GA4/Plausible 분석 도구, support@/privacy@ 이메일 SMTP 발송, vercel.ts(@vercel/config) 마이그레이션 검토.
- 2026-05-05: **M9 UX 보강 완료**. 의존성 추가 없음. 신규 — `components/ui/{toast,use-toast,toaster,dropdown-menu}.tsx` (shadcn 패턴 + Zustand 단일 스토어, max 3 / 5s default · 7s destructive), `components/theme/{ThemeProvider,ThemeToggle}.tsx` (system/light/dark + localStorage + FOUC 방지 inline 스크립트), `components/admin/ResourceDeleteDialog.tsx`, `lib/address/use-postcode.ts` (Daum SDK 동적 로드 idempotent), `lib/admin/resource-usage.ts` (jsonb::text ilike 보수적 매칭). 수정 — `app/layout.tsx`(ThemeProvider+Toaster 마운트, head inline init script), `components/layout/HeaderClient.tsx`(데스크탑/모바일 모두 ThemeToggle 추가), `app/api/admin/resources/[id]/route.ts` DELETE(`?force=true` 미설정 시 사용중 검사 → 409 RESOURCE_IN_USE + details {usedInPages, usedInCovers, keywords}), `OrderForm.tsx`(usePostcode 통합 — alert 제거), 5개 admin 클라(`ResourcesClient`/`BookSizeRowActions`/`UsersClient`/`OrderActions`/`ExportClient`)의 alert 16건 → toast 일괄 치환. lint 경고 2건 해소(`FabricStage` addClipart deps에 `heightMm` 추가, `lib/fabric/snap.ts` `import * as fabric` → `import type * as fabric`). pnpm typecheck/lint/test/build 모두 통과 (lint 0 warnings, test 106 passed/1 skipped).
- 2026-05-05: **M12 편집기 UX 보강 완료**. 의존성 추가 없음 (네이티브 Pointer Events 로 DnD 구현). 신규 — `supabase/migrations/0013_pages_reorder_rpc.sql` (`reorder_project_pages` 2단계 음수 임시이동 RPC + `shift_pages_after` ±N 시프트 RPC, service_role 만 EXECUTE), `app/api/pages/reorder/route.ts` (Zod 검증 + 소유권 + 페이지 ID 일치성 + 중복 검사 → RPC), `app/api/pages/insert/route.ts` (afterPageNo 후 page_no 시프트 + 빈 PageDoc 생성 — collage 는 `buildCollagePage` placeholderSlot 만, polaroid 는 빈 objects), `lib/fabric/clipboard.ts` (모듈 싱글톤 `fabricClipboard` — copy/read/clear, 분기형 `ClipboardLayoutData` 로 discriminated union 보존, +5mm offset 누적), `lib/fabric/clipboard.test.ts` (8 tests — copy/read 라운드트립 + 누적 offset + clear), `components/editor/KeyboardShortcutsHelp.tsx` (Radix Dialog + macOS/Windows mod key 자동 감지 + `useShortcutsAutoShow` localStorage 첫방문 자동 노출). 수정 — `app/api/pages/[id]/route.ts` DELETE 메서드 추가 (소유권 후 admin 삭제 + shift_pages_after p_shift=-1), `components/editor/FabricStage.tsx` (handle 에 `pasteLayoutObject` / `duplicateSelected` / `getDpi` / `getBleedMm` 추가 — pageDocToFabric + bleed 보정 재사용), `app/(user)/editor/[projectId]/PreviewGrid.tsx` (Pointer Events DnD: 마우스 8px threshold / 모바일 500ms long-press + vibrate, 카드 ⋯ DropdownMenu — 추가/삭제, "새 페이지 추가" 빈 카드, 키보드 Backspace/Delete 삭제 다이얼로그, 낙관적 업데이트), `app/(user)/editor/[projectId]/EditorClient.tsx` (handleReorder/Insert/Delete 통합 + busy 상태 + toast), `app/(user)/editor/[projectId]/pages/[pageId]/PageEditor.tsx` (Cmd/Ctrl+C/V/D 단축키 + J/K/PageUp/PageDown 페이지 점프 + `?` 단축키 안내 + ⌨ 헤더 버튼 + 페이지 번호 DropdownMenu 점프 + 입력 폼/textarea/contentEditable 가드 + 첫 방문 1.2초 후 자동 노출), `app/(user)/editor/[projectId]/pages/[pageId]/page.tsx` (siblings prop 전달 — 점프 드롭다운용), `lib/db/types.ts` (Functions 에 `reorder_project_pages`/`shift_pages_after` 타입 등록). 검증 — typecheck/lint(0)/test(13 files / 119 passed / 1 skipped — clipboard 8 tests 신규)/build 모두 통과. 후속 — 모바일 햅틱은 navigator.vibrate 가 지원될 때만 시도 (iOS Safari 미지원), 사용자 가이드 영상은 별도 마케팅 작업으로 분리.
- 2026-05-05: **M15 페이지 미리보기 + 이메일 인프라 완료**. 의존성 추가 없음. 신규 — `supabase/migrations/0015_email_jobs.sql` (email_jobs 테이블 + RLS — admin SELECT, service_role write — `set_updated_at` 트리거 재사용), `app/api/pages/[id]/preview/route.ts` (GET/POST 모두 — 표지 미리보기 패턴 그대로 — renderPageToPng dpi=72 + photo/clipart resolver + 폰트 등록 + Cache-Control: private, max-age=60), `components/editor/PagePreviewDialog.tsx` (Radix Dialog — fetch + 로딩/에러 상태 + PNG 다운로드 + bleed 가이드 점선 옵션), `lib/email/templates.ts` (8 템플릿 — order 6종 + user 2종, 평문 + 한국어 HTML 인라인 스타일, `TEMPLATE_BY_ORDER_STATUS` 매핑, CJ대한통운/롯데/한진/우체국 추적 URL helper), `lib/email/queue.ts` (`enqueueEmail` — service_role INSERT, 실패 시 throw 안 함 + 콘솔 로그), `lib/email/worker.ts` (`processEmailQueue` — pending/failed batch 10 — claim race 보호 — sendEmail stub: SMTP_HOST/RESEND_API_KEY 미설정이면 cancelled 마킹), `app/api/cron/process-emails/route.ts` (Vercel Cron */5 * * * * — `Authorization: Bearer ${CRON_SECRET}` 또는 `x-vercel-cron: 1` 검증), `app/admin/emails/{page,EmailsClient}.tsx` (필터/검색/페이지네이션 50건씩 + 재시도 액션), `app/api/admin/emails/route.ts` (GET 리스트 + 필터), `app/api/admin/emails/[id]/retry/route.ts` (failed/cancelled → pending reset + attempt=0 + audit log action='email.retry'), `lib/email/{templates,queue}.test.ts` (15 + 4 tests, server-only 모킹). 수정 — `app/(user)/editor/[projectId]/pages/[pageId]/PageEditor.tsx` (Cmd/Ctrl+Shift+P 단축키 + Eye 아이콘 헤더 버튼 + dirty 시 자동저장 후 다이얼로그 — save deps 추가), `app/(user)/editor/[projectId]/PreviewGrid.tsx` (카드 ⋯ 메뉴에 미리보기 항목 추가 + PagePreviewDialog 마운트), `components/editor/KeyboardShortcutsHelp.tsx` ("페이지 미리보기" 단축키 라인 추가), `app/api/payments/confirm/route.ts` (PDF 빌드 후 order.paid 메일 enqueue — Promise.all 로 profile/project/pageCount 일괄 + book_size.name 룩업 + try/catch 로 confirm 응답 보호), `app/api/admin/orders/[id]/transition/route.ts` (transition 후 TEMPLATE_BY_ORDER_STATUS 매핑으로 enqueue — shipped 시 trackingNo/Carrier 컨텍스트), `app/api/account/delete/route.ts` (anonymize 전 user.account_deleted 메일 캐치), `app/api/auth/agree/route.ts` (`profiles.terms_agreed_at` 가 NULL → 첫 동의 → user.welcome enqueue), `app/admin/AdminSidebar.tsx` ("이메일 잡" 메뉴 추가), `vercel.json` (functions 에 cron 라우트 maxDuration=60 + crons 섹션 `*/5 * * * *`), `.env.production.example` (CRON_SECRET + RESEND_API_KEY/SMTP_* 후속 슬롯), `lib/db/types.ts` (EmailJob/EmailJobStatus + Database.email_jobs 등록), `vitest` 에서는 queue.test 가 server-only 모듈을 vi.mock 으로 stub. 검증 — typecheck(0)/lint(0 warnings)/test(15 files / 153 passed / 1 skipped — 19개 신규 테스트)/build(67 라우트 등록) 모두 통과. SMTP 미통합 — Phase 12 후속에서 `lib/email/worker.ts` 의 `sendEmail()` 분기에 Resend SDK 또는 nodemailer 통합 (TODO 주석 명시). 다국어/카카오 알림톡은 별도 마일스톤.

- 2026-05-14: **M-내비최적화 페이지 이동 속도 개선 완료**. 의존성 추가 없음. 두 단계로 진행 — Phase 1: (a) `next.config.mjs` `experimental.staleTimes { dynamic: 30, static: 180 }` 추가 — App Router 클라이언트 Router Cache TTL 활성화로 다녀온 페이지 재방문 시 prefetch RSC payload 즉시 재사용, (b) 누락 `loading.tsx` 8개 신규 — `/mypage/orders/[orderId]`, `/mypage/photos`, `/mypage/trash`, `/mypage/account`, `/mypage/points`, `/order/[projectId]`, `/cover/[projectId]`, `/login` 각각 페이지 진입 즉시 pulse 스켈레톤 표시 (TTFP 단축), (c) `mypage/orders/[orderId]` 의 reviews(id) inline join 으로 단일 쿼리 (별도 select 1회 제거 → 평균 50~100ms 단축). Phase 2: (d) `/terms`·`/privacy`·`/refund` 에 `force-static` + `revalidate=false` 명시 — 빌드 결과 `ƒ Dynamic` → `○ Static` 으로 prerender, CDN edge 캐시 활용 TTFB ~0ms, (e) PWA Service Worker v1 → v2 SWR(Stale-While-Revalidate) 패턴 — 공개 페이지(/, /gallery, /terms, /privacy, /refund, /offline)만 캐시 즉시 반환 + 백그라운드 fetch + 보호 페이지(mypage/admin/editor/cover/order)는 network-first 유지하여 인증 stale 데이터 노출 방지, `CACHE_NAME` "100p-v1" → "100p-v2" 로 자동 무효화, (f) `/mypage` 진입 카운트 4종(orders/projects/active-photos/trash-photos)을 `get_user_dashboard_counts(p_user_id)` 단일 RPC 로 묶어 RTT 2회 → 1회 (마이그레이션 `0024_user_dashboard_counts.sql`, SECURITY DEFINER + service_role EXECUTE), `lib/db/types.ts` RPC 시그니처 등록. 운영 활성화 — 사용자가 Supabase SQL Editor 에서 0024 마이그레이션 수동 적용 필요(미적용 시 카운트 0 fallback, 페이지 자체는 정상). 검증 — pnpm typecheck(0)/test 153/build (legal 3 페이지 ○ Static 확인)/preview /terms 정상.

- 2026-05-14: **M-홈리뉴얼 §3·§4 사진 배경 카드 리디자인 완료**. 의존성 추가 없음. 신규 — `components/home/FeatureCards.tsx` ("완벽한 포토북을 위한 세 가지 핵심" 3카드 — 풀블리드 Unsplash 사진 + 다크 그라디언트 + amber 호버 라인 + aspect 4/5↔3/4 + 1.06x 호버 줌 + 헤더 "Why 100p Books" 키커 + amber underline), `components/home/BookSizeCards.tsx` ("나에게 딱 맞는 사이즈" 3카드 — 사이즈별 비율 미니·A5 3:5 세로 / 스퀘어 1:1 + 좌상단 backdrop-blur 사이즈 뱃지 + 하단 amber 라인 호버 + 카드 1.5px hover lift + 화살표 →→ 이동). 수정 — `tailwind.config.ts` `fade-up` keyframes + animation 0.7s cubic-bezier(0.22,1,0.36,1) 추가, `app/(user)/page.tsx` 인라인 FEATURES/BOOK_SIZES 데이터·마크업 제거 + 컴포넌트 호출로 대체 + 사용 안 하는 import(Sparkles/Camera/Images) 정리. 사진 6장 사용자 피드백 반영 — 카메라/풍경 톤 → 앨범·사진집·포토북·화보집 주제로 전부 교체(폴라로이드 collection / 빈티지 앨범 / 사진집 디테일 / 작은 사진집 / 정사각 사진집 / A5 화보집). 기술 결정 — framer-motion `useInView`/`whileInView` 가 preview controlled 브라우저의 `winH=0` 버그로 trigger 안 되는 환경 의존 문제를 발견하여 CSS-only animation + tailwind `animate-fade-up` + `animationDelay` 카드별 120ms stagger 로 변경(RSC 호환 + framer-motion 의존 0 + 운영 안정성↑). 검증 — pnpm typecheck(0)/test 153/build 31 라우트/Playwright desktop 6/6/preview DOM article 403×537 · image 422 natural · opacity 1.

- 2026-05-13: **M5-패치 PDF borderRadius + shadow 클립 해소 완료**. M5 이월 한계 — `render-page.ts` drawPhoto 가 한 번의 clip() 안에서 shadow 와 drawImage 를 모두 처리해 그림자가 clip 경계 바깥으로 새지 못해 잘려 보이던 문제. 수정 — 2-pass 패스 분리: Pass 1 (shadow only) clip 적용 전 슬롯 모양을 흰색 fill 로 한 번 그려서 그림자만 캔버스에 미리 새김 (cover 모드는 이미지가 덮으므로 흰 fill 비가시 / contain 모드는 빈 공간이 흰색으로 자연스러움), save/restore 로 shadow 상태 격리, Pass 2 (image) roundRect clip 후 drawImage shadow 비활성. 검증 보강 — `scripts/verify-pdf.ts` 에 photo + borderRadius 8mm + shadow blur 3mm + offsetY 1.5mm 케이스 추가 (32×32 더미 PNG base64 inline), 결과 2 페이지 / 67,542 bytes / 834ms / PDF 1.7 / `/Subtype /Image` 2개 정상 임베드. 부수 — `lib/pdf/build.test.ts` 의 `PDF_NATIVE_TEST` 케이스에 vitest jsdom + @napi-rs/canvas.toBuffer NaN 환경 의존 버그 안내 주석 + 대안(`pnpm verify:pdf`) 명시.

- 2026-05-13: **M8-보강 QA 측정 완료**. 의존성 추가 — `@playwright/test@^1.60`. 신규 — `scripts/verify-pdf.ts` (145mm sq · text 2 + rect 1 · 40KB / 353ms · PDF 1.7 헤더 검증), `scripts/_server-only-stub.cjs` + `scripts/_empty.cjs` (tsx 로 server-only 모듈 import 시 throw 우회 — Module._resolveFilename 패치), `tests/mocks/server-only.ts` (vitest 환경 alias 용 빈 stub), `playwright.config.ts` (chromium-desktop + mobile-chromium 2 프로젝트, webServer 자동 dev / PLAYWRIGHT_BASE_URL 외부 URL 지원, iPhone 13 webkit 회피 — CI 부담 최소화), `e2e/smoke.spec.ts` (6 케이스 — 홈/upload(가드)/gallery/login/mypage/mypage/points). 수정 — `vitest.config.ts` (server-only alias 추가, jsdom 환경에서도 import 가능), `package.json` (verify:pdf / e2e / e2e:headed 스크립트 추가), `.gitignore` (tmp/ playwright-report/ test-results/ 추가), `STATUS.md` M8 마일스톤 🟡 일부 완료로 갱신 + 측정 결과 섹션 추가. 검증 — pnpm typecheck(0)/test(15 files / 153 passed / 1 skipped)/build(31 라우트 정상) + Playwright 12/12 통과 (5.7s) + Lighthouse 모바일 Performance 97/100, LCP 1.5s, FCP 1.3s, TBT 10ms, CLS 0 (Core Web Vitals 전부 통과). Speed Index 4.8s / TTI 7.3s 는 클라이언트 JS 청크 영향이며 LCP 무관. 산출물 `tmp/lighthouse/100pbooks.report.html` (gitignored). 운영 배포 — GitHub push → auto-deploy → `dpl_6qDviu7p... READY` (커밋 `5e67ac4`).

- 2026-05-13: **M16-종합 마무리 (할인 코드 결제 적용 + 후기 작성 + 포인트 내역 페이지) 완료**. 의존성 추가 없음. 발견 — 코드 베이스 점검 결과 (1) `/api/discounts/validate` + `OrderForm` 클라 호출 + `payments/confirm` 의 `discount_uses` INSERT + `increment_discount_used` RPC 가 이미 완전 구현되어 있었음 → STATUS.md 의 "❌ 미착수" 표기를 ✅ 완료로 정정, (2) `ReviewDialog` 컴포넌트가 이미 존재하고 `/mypage/orders` 리스트에 연결되어 있었음 → 주문 상세 페이지로 확장만 추가, (3) `PointHistoryCard` 컴포넌트는 마이페이지에 인라인 마운트되어 있었으나 전용 페이지 미존재. 신규 — `app/(user)/mypage/points/page.tsx` (최근 200건 전용 페이지, requireUser 가드). 수정 — `app/(user)/mypage/orders/[orderId]/page.tsx` (shipped/delivered 주문에 `ReviewDialog` 노출 + 이미 작성 시 "후기 작성 완료" 뱃지, reviews 테이블 선조회로 중복 방지), `app/(user)/mypage/page.tsx` (포인트 내역 카드를 그리드에 추가 + 인라인 PointHistoryCard 는 limit=20 + showViewAll 로 전체 보기 링크), `components/mypage/PointHistoryCard.tsx` (limit prop + showViewAll prop 추가 — useCallback deps 동기화 + Link 임포트). 검증 — pnpm typecheck(0)/test(15 files / 153 passed)/build(/mypage/points 4.75 kB / First Load 108 kB 정상 등록) + preview 서버 인증 가드 동작 확인(`?next=/mypage/points` 리다이렉트). 운영 배포 — `dpl_2DU59GNq...` READY (커밋 `eadbb29`). 비고 — 첫 vercel CLI 직접 호출 시 worktree 의 `.vercel/project.json` 자동 생성으로 잘못된 프로젝트(`frosty-haibt-512d2b`)로 배포되는 사고 발생 → `.vercel/project.json` 정정 후 GitHub auto-deploy 가 정상 처리. 재발 방지로 메모리에 "worktree 에서 vercel --prod 직접 실행 금지" 피드백 저장.

- 2026-05-09: **M16-7 카카오 OAuth 활성화 + 포인트 거래 내역(ledger) 완료**. 의존성 추가 없음. 신규 — `supabase/migrations/0023_point_ledger.sql` (point_ledger 테이블 + RLS — 본인 SELECT, service_role write — `add_user_points_v2`/`deduct_user_points_v2`/`award_referral_reward_v2` RPC: ledger 자동 기록 + 거래 후 잔액 반환 + reason/ref_type/ref_id/memo + `sync_oauth_profile` RPC: auth.users.raw_user_meta_data 의 카카오/구글 nickname/profile_image → profiles.display_name/avatar_url 동기화 (기존 값이 비어있을 때만 채움) + profiles 컬럼 추가 `avatar_url`/`oauth_provider`), `components/mypage/PointHistoryCard.tsx` (잔액 + 누적 적립/사용 + 최근 20건 거래 내역 — relative date / 적립=초록 ↗ / 사용=빨강 ↘ / 새로고침 버튼), `CLAUDE.local.md` (gitignore 포함 — 카카오 콘솔 + Supabase Provider 설정 가이드 / 트러블슈팅 / 포인트 운영 SQL 스니펫). 수정 — `app/(auth)/login/LoginForm.tsx` (주석 처리되어 있던 카카오 버튼 활성화 + scopes='profile_nickname profile_image' + 약관 미동의 시 disabled + oauth 진행 중 로딩 표시 + 인라인 카카오 SVG), `app/api/auth/callback/route.ts` (record_agreements 후 sync_oauth_profile RPC 호출 — best-effort), `app/api/points/route.ts` (잔액만 반환 → balance + ledger 최근 N건 + totals.earned/spent + reason→한글 라벨 매핑 + ?limit=N 쿼리 지원), `app/api/attendance/check/route.ts` (add_user_points → add_user_points_v2 — reason='attendance'/'attendance_bonus' + memo 자동 기록), `app/api/payments/confirm/route.ts` (deduct_user_points → deduct_user_points_v2 — reason='order_use' + ref_type='orders' / award_referral_reward → award_referral_reward_v2), `app/(user)/mypage/page.tsx` (PointHistoryCard 마운트 — ReferralCard 아래), `lib/db/types.ts` (Profile 에 avatar_url/oauth_provider 추가 + PointLedger/PointLedgerReason 타입 + Database.point_ledger 테이블 + Functions 4 RPC 등록). 검증 — pnpm typecheck/lint(0)/test(15 files / 153 passed / 1 skipped — 모두 통과)/build(70+ 라우트 등록, First Load JS 87.4kB) 모두 통과. 배포 전 사용자 액션 — Kakao Developers 콘솔에서 앱 생성 + Web 플랫폼 등록 + Redirect URI 에 Supabase Callback URL 입력 + 동의 항목(profile_nickname/profile_image) 활성화, Supabase Dashboard 에서 Auth → Providers → Kakao 활성화 + REST API Key/Client Secret 입력, Auth → URL Configuration 에 Site URL 및 Redirect URLs 등록. 운영 DB 에 0023 마이그레이션 적용 필요.

- 2026-04-25: **M8 QA & 폴리싱 완료**. 정적 분석 — 28개 API 라우트, 9개 마이그레이션, 50+ 페이지/컴포넌트. 보안 PASS(`service_role`/`TOSS_SECRET_KEY` 클라 노출 0, EXIF GPS 차단, RLS 7테이블 모두 enable, PageDoc/amount 이중 검증, auth callback open-redirect 방어), a11y PASS+권고1(html lang ko / focus-visible / prefers-reduced-motion / 토큰화). 패치 1건: `FileGridItem.tsx` remove 버튼 터치 타겟 `size-8`(32px) → `size-11`(44px) + focus-visible ring. 배포 차단 이슈 없음. 산출물 `QA_REPORT.md` 신설(배포 전 사용자 액션 + 회귀 체크리스트 포함). P1 잔여(인메모리 SSE→Redis, Vercel 60s 제한, 카카오 OAuth provider, 첫 admin SQL), P2 폴리싱(인라인 alert→Toast 16건, CMYK/ICC, 우편번호 SDK)는 배포 후 진행.

## M0 산출물 (요약)
**백엔드**: package.json / tsconfig / next.config / .env.example / ESLint / Vitest / .gitignore / 3개 SQL 마이그레이션(스키마+RLS+시드) / lib/db/{server,browser,admin,types} / lib/auth/session / middleware / auth 콜백 / health API / response 헬퍼
**프론트**: tailwind.config / postcss / globals.css (Pretendard + Playfair) / lib/utils / app/layout / loading / not-found / (user) layout + 랜딩 + HeroMotion / (auth) layout + login + LoginForm / UI primitives (button/input/card) / Header + HeaderClient + Footer + MobileBottomSheet

## 빌드/테스트 검증 (2026-04-25)

✅ `pnpm install` (15.5초) — 모든 의존성 정상 설치 (canvas 네이티브 빌드 실패는 jsdom 선택적 의존성으로 무시 가능, 우리는 `@napi-rs/canvas` 사용)
✅ `pnpm typecheck` — 타입 에러 0건
✅ `pnpm lint` — 에러 0건 (경고 2건: FabricStage useCallback deps, snap.ts type-only import — 무해)
✅ `pnpm test` — 12 파일 / 106 통과 / 1 skip (PDF 통합 테스트는 `PDF_NATIVE_TEST=1` 환경에서만 실행)
✅ `pnpm build` — Production 빌드 성공. 모든 라우트 생성 완료. First Load JS shared 87.3kB.

### 검증 중 적용된 수정사항
- `next.config.ts` → `next.config.mjs` (Next 14는 .ts 미지원)
- `@supabase/ssr@0.5.2` + `@supabase/supabase-js@2.45.6` 핀 (v2.105 타입 호환성 이슈 회피)
- `noUncheckedIndexedAccess` 관련 undefined 단언 (build.ts, render-page.ts, cover.test.ts, build.test.ts, admin/resources.ts, admin/resources/[type]/page.tsx)
- Fabric v6 이벤트 핸들러 타입 우회 (snap.ts, FabricStage.tsx)
- Buffer<ArrayBufferLike> ↔ BodyInit/Buffer 캐스팅 (orders/export, excel.test.ts)
- excel.ts에서 `server-only` 제거 (테스트 환경 호환, exceljs는 노드 의존이지만 server-only 마커는 불필요)
- validate.ts: HEIC 확장자 감지 로직이 빈 MIME 케이스에서 먼저 동작하도록 수정
- lint 에러: `prefer-const`, `no-this-alias` 규칙 위반 수정

## 후속 이월 이슈
- [L] `middleware.ts`의 비-admin 경로 `getUser()` 호출 최적화 (필요 시 M6에서)
- 카카오 OAuth provider 등록 후 LoginForm 주석 해제 (사용자 인프라 작업)
- [M] M5 진행률 SSE: 인메모리 `lib/pdf/jobs.ts` 레지스트리 → multi-instance(Vercel) 환경에서는 SSE 가 다른 인스턴스로 라우팅될 수 있음. 운영 시 Redis/Upstash 로 교체 필요.
- [L] M5 PDF: borderRadius + photo shadow 동시 적용 시 그림자가 클립으로 잘리는 한계 (render-page.ts 주석). 별도 단계로 그림자 패스 분리하면 해결 가능.
- [L] M5 PDF: CMYK 변환 / ICC 프로파일은 sRGB 출력으로 시작. 인쇄소 요구 시 ghostscript 후처리 또는 sharp 의 ICC pipeline 추가.

## 오토파일럿 실행 명령

```
Agent(subagent_type="orchestrator",
      prompt="PLAN.md와 PROGRESS.md를 읽고, 다음 미완료 마일스톤을 식별해 담당 에이전트에 위임하세요. 완료 시 PROGRESS.md 업데이트.")
```
